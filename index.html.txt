<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>GEMPAR — Gerakan Memilah Sampah Dari Rumah</title>
  <meta name="description" content="Pilah sampah dari rumah, dapatkan GEM, dan tukarkan banyak reward. Gerakan memilah sampah dari rumah untuk lingkungan lebih bersih.">
  <meta name="theme-color" content="#22c55e">
  <link rel="manifest" href="manifest.json">
  <link rel="icon" type="image/png" sizes="192x192" href="assets/icon-192.png">
  <link rel="apple-touch-icon" href="assets/icon-192.png">
  <style>
    :root {
      --bg: #0f1115;
      --bg-elevated: #181a20;
      --bg-card: #1e2028;
      --green: #22c55e;
      --green-hover: #16a34a;
      --text: #f0f0f0;
      --text-muted: #a0a0a0;
      --radius: 14px;
      --shadow: 0 8px 32px rgba(0,0,0,0.4);
      --shadow-card: 0 4px 20px rgba(0,0,0,0.35);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    html {
      scroll-behavior: smooth;
      -webkit-tap-highlight-color: transparent;
    }

    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    /* ── Install Banner ── */
    #install-bar {
      position: fixed;
      top: 0; left: 0; right: 0;
      background: var(--bg-elevated);
      border-bottom: 1px solid rgba(255,255,255,0.06);
      padding: 14px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      z-index: 1000;
      transform: translateY(-100%);
      transition: transform 0.5s cubic-bezier(0.22, 1, 0.36, 1);
    }
    #install-bar.show { transform: translateY(0); }
    #install-bar .install-text {
      font-size: 0.9rem;
      font-weight: 500;
      color: var(--text);
    }
    #install-bar .install-btn {
      background: var(--green);
      color: #fff;
      border: none;
      padding: 8px 18px;
      border-radius: 10px;
      font-size: 0.85rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s, transform 0.2s;
    }
    #install-bar .install-btn:hover { background: var(--green-hover); transform: scale(1.04); }
    #install-bar .install-close {
      background: none;
      border: none;
      color: var(--text-muted);
      font-size: 1.2rem;
      cursor: pointer;
      margin-left: 10px;
      padding: 4px;
      transition: color 0.2s;
    }
    #install-bar .install-close:hover { color: var(--text); }

    /* ── Hero ── */
    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 100px 24px 40px;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeUp 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .mascot-wrap {
      width: 160px;
      height: 160px;
      margin-bottom: 24px;
      border-radius: 50%;
      overflow: hidden;
      background: var(--bg-elevated);
      box-shadow: var(--shadow);
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .mascot-wrap img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .hero h1 {
      font-size: clamp(1.6rem, 5vw, 2.2rem);
      font-weight: 700;
      line-height: 1.25;
      max-width: 420px;
      margin-bottom: 14px;
      letter-spacing: -0.02em;
    }
    .hero h1 .accent { color: var(--green); }

    .hero p {
      font-size: 1rem;
      color: var(--text-muted);
      max-width: 380px;
      margin-bottom: 32px;
      line-height: 1.6;
    }

    .cta-btn {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: var(--green);
      color: #fff;
      text-decoration: none;
      font-size: 1rem;
      font-weight: 600;
      padding: 14px 32px;
      border-radius: var(--radius);
      box-shadow: 0 4px 20px rgba(34,197,94,0.25);
      transition: transform 0.25s cubic-bezier(0.22, 1, 0.36, 1),
                  box-shadow 0.25s ease,
                  background 0.2s ease;
      cursor: pointer;
      border: none;
    }
    .cta-btn:hover {
      transform: scale(1.04);
      background: var(--green-hover);
      box-shadow: 0 6px 28px rgba(34,197,94,0.35);
    }
    .cta-btn:active { transform: scale(0.98); }
    .cta-btn svg { width: 18px; height: 18px; fill: currentColor; }

    /* ── Carousel ── */
    .carousel-section {
      padding: 20px 24px 40px;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeUp 0.8s 0.15s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .carousel {
      position: relative;
      width: 100%;
      max-width: 520px;
      margin: 0 auto;
      aspect-ratio: 16 / 10;
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: var(--shadow-card);
      background: var(--bg-card);
    }

    .carousel img {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0;
      transition: opacity 0.8s ease-in-out;
      border-radius: var(--radius);
    }
    .carousel img.active { opacity: 1; }

    .carousel-dots {
      display: flex;
      justify-content: center;
      gap: 8px;
      margin-top: 16px;
    }
    .carousel-dots button {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      border: none;
      background: rgba(255,255,255,0.15);
      cursor: pointer;
      transition: background 0.3s, transform 0.3s;
    }
    .carousel-dots button.active {
      background: var(--green);
      transform: scale(1.3);
    }

    /* ── Value Info ── */
    .value-section {
      padding: 20px 24px 40px;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeUp 0.8s 0.3s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .value-grid {
      display: flex;
      justify-content: center;
      gap: 12px;
      flex-wrap: wrap;
      max-width: 480px;
      margin: 0 auto;
    }

    .value-chip {
      background: var(--bg-elevated);
      border: 1px solid rgba(255,255,255,0.06);
      padding: 14px 24px;
      border-radius: 12px;
      font-size: 0.9rem;
      font-weight: 500;
      color: var(--text);
      display: flex;
      align-items: center;
      gap: 10px;
      transition: transform 0.2s, border-color 0.2s;
    }
    .value-chip:hover { transform: translateY(-2px); border-color: rgba(34,197,94,0.3); }
    .value-chip .dot { width: 8px; height: 8px; border-radius: 50%; background: var(--green); }

    /* ── Footer ── */
    .footer {
      text-align: center;
      padding: 40px 24px 120px;
      opacity: 0;
      animation: fadeUp 0.8s 0.45s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }
    .footer p {
      font-size: 0.8rem;
      color: rgba(255,255,255,0.2);
      letter-spacing: 0.05em;
    }

    /* ── FABs ── */
    .fab-container {
      position: fixed;
      bottom: 24px;
      right: 20px;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 900;
    }

    .fab {
      display: flex;
      align-items: center;
      gap: 10px;
      background: var(--bg-elevated);
      border: 1px solid rgba(255,255,255,0.08);
      color: var(--text);
      text-decoration: none;
      padding: 12px 18px;
      border-radius: 14px;
      font-size: 0.88rem;
      font-weight: 600;
      box-shadow: 0 8px 24px rgba(0,0,0,0.5);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      transition: transform 0.25s cubic-bezier(0.22, 1, 0.36, 1),
                  box-shadow 0.25s ease,
                  border-color 0.2s;
      cursor: pointer;
      animation: float 4s ease-in-out infinite;
    }
    .fab:hover {
      transform: scale(1.05);
      box-shadow: 0 12px 32px rgba(0,0,0,0.6);
      border-color: rgba(34,197,94,0.4);
    }
    .fab:active { transform: scale(0.97); }
    .fab img { width: 22px; height: 22px; object-fit: contain; }
    .fab:nth-child(2) { animation-delay: 0.5s; }

    /* ── Animations ── */
    @keyframes fadeUp {
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-4px); }
    }

    /* ── Responsive ── */
    @media (max-width: 360px) {
      .hero { padding-top: 90px; }
      .mascot-wrap { width: 130px; height: 130px; }
      .value-chip { padding: 12px 18px; font-size: 0.85rem; }
      .fab { padding: 10px 14px; font-size: 0.82rem; }
      .fab img { width: 18px; height: 18px; }
    }
  </style>
</head>
<body>

  <!-- Install Banner -->
  <div id="install-bar">
    <span class="install-text">Install GEMPAR App</span>
    <div style="display:flex;align-items:center;">
      <button class="install-btn" id="install-btn">Install</button>
      <button class="install-close" id="install-close">&times;</button>
    </div>
  </div>

  <!-- Hero -->
  <section class="hero">
    <div class="mascot-wrap">
      <img src="assets/mascot.png" alt="GEMPAR Mascot" loading="eager">
    </div>
    <h1>
      Pilah Sampah dari Rumah, Dapat <span class="accent">GEM</span>, Banyak Manfaat
    </h1>
    <p>
      Mulai dari rumah untuk lingkungan lebih bersih dan masa depan lebih baik
    </p>
    <a href="https://gempar212.base44.app" class="cta-btn">
      <svg viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zm0 9l2.5-1.25L12 8.5l-2.5 1.25L12 11zm0 2.5l-5-2.5-5 2.5L12 22l10-8.5-5-2.5-5 2.5z"/></svg>
      Masuk ke Aplikasi
    </a>
  </section>

  <!-- Carousel -->
  <section class="carousel-section">
    <div class="carousel" id="carousel">
      <img src="assets/slide1.jpg" alt="Slide 1" class="active" loading="lazy">
      <img src="assets/slide2.jpg" alt="Slide 2" loading="lazy">
      <img src="assets/slide3.jpg" alt="Slide 3" loading="lazy">
    </div>
    <div class="carousel-dots" id="dots"></div>
  </section>

  <!-- Value Info -->
  <section class="value-section">
    <div class="value-grid">
      <div class="value-chip"><span class="dot"></span>Pilah Sampah</div>
      <div class="value-chip"><span class="dot"></span>Dapat GEM</div>
      <div class="value-chip"><span class="dot"></span>Tukar Reward</div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <p>&copy; 2026 GEMPAR. Gerakan Memilah Sampah Dari Rumah.</p>
  </footer>

  <!-- FABs -->
  <div class="fab-container">
    <a href="https://gempar212.base44.app" class="fab">
      <img src="assets/icon1.png" alt="">
      Gempar 1
    </a>
    <a href="https://gemparwarakas-v2.base44.app" class="fab">
      <img src="assets/icon2.png" alt="">
      Gempar 2
    </a>
  </div>

  <script>
    // ── Carousel ──
    (function() {
      const slides = document.querySelectorAll('#carousel img');
      const dotsContainer = document.getElementById('dots');
      let current = 0;
      const total = slides.length;

      // Create dots
      slides.forEach((_, i) => {
        const btn = document.createElement('button');
        btn.setAttribute('aria-label', 'Slide ' + (i + 1));
        if (i === 0) btn.classList.add('active');
        btn.addEventListener('click', () => goTo(i));
        dotsContainer.appendChild(btn);
      });
      const dots = dotsContainer.querySelectorAll('button');

      function goTo(index) {
        slides[current].classList.remove('active');
        dots[current].classList.remove('active');
        current = index;
        slides[current].classList.add('active');
        dots[current].classList.add('active');
      }

      function next() {
        goTo((current + 1) % total);
      }

      setInterval(next, 3500);
    })();

    // ── PWA Install Banner ──
    (function() {
      let deferredPrompt = null;
      const bar = document.getElementById('install-bar');
      const installBtn = document.getElementById('install-btn');
      const closeBtn = document.getElementById('install-close');

      window.addEventListener('beforeinstallprompt', (e) => {
        e.preventDefault();
        deferredPrompt = e;
        bar.classList.add('show');
        setTimeout(() => bar.classList.remove('show'), 5000);
      });

      installBtn.addEventListener('click', async () => {
        if (!deferredPrompt) return;
        deferredPrompt.prompt();
        const { outcome } = await deferredPrompt.userChoice;
        if (outcome === 'accepted') {
          bar.classList.remove('show');
        }
        deferredPrompt = null;
      });

      closeBtn.addEventListener('click', () => {
        bar.classList.remove('show');
      });

      // Auto-hide fallback
      window.addEventListener('load', () => {
        if (!deferredPrompt) {
          setTimeout(() => bar.classList.remove('show'), 6000);
        }
      });
    })();

    // ── Service Worker ──
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('sw.js')
          .then(reg => console.log('SW registered:', reg.scope))
          .catch(err => console.log('SW failed:', err));
      });
    }
  </script>
</body>
</html>
